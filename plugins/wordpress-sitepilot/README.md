# SitePilot WordPress Plugin

This directory is reserved for the thin SitePilot companion plugin.

Implementation starts in task `T11`. For now, the folder exists so the repository shape is fixed early.
